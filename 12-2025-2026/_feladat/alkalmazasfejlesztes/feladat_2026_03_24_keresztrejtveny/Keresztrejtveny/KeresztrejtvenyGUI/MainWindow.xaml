<Window x:Class="KeresztrejtvenyGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeresztrejtvenyGUI"
        mc:Ignorable="d"
        Title="Keresztrejtvény készítő" Height="550" Width="600"
        Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Méret kiválasztás -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Keresztrejtvény mérete (sor x oszlop):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="cmbSorok" Width="50" Margin="0,0,5,0"/>
            <ComboBox x:Name="cmbOszlopok" Width="50"/>
        </StackPanel>

        <!-- Gombok -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnLetrehozas" Content="Keresztrejtvény létrehozása" Padding="10,5" Margin="0,0,10,0" Click="BtnLetrehozas_Click"/>
            <Button x:Name="btnMentes" Content="Keresztrejtvény mentése" Padding="10,5" Margin="0,0,10,0" Click="BtnMentes_Click"/>
            <ComboBox x:Name="cmbIndex" Width="50"/>
        </StackPanel>

        <!-- Rács konténer -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="gridRacs"/>
        </ScrollViewer>
    </Grid>
</Window>

