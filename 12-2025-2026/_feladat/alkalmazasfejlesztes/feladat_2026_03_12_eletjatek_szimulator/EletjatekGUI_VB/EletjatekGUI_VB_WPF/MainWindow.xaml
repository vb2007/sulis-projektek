<Window x:Class="EletjatekGUI_VB_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EletjatekGUI_VB_WPF.ViewModel"
        xmlns:local="clr-namespace:EletjatekGUI_VB_WPF"
        mc:Ignorable="d"
        Title="Életjáték álláskészítő" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <DockPanel>
        <StackPanel Margin="0,20,0,0" DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Margin="5,0,0,0" Content="Matrix mérete [sor x oszlop]:" />
            <ComboBox SelectedValue="{Binding Rows, Mode=TwoWay}" ItemsSource="{Binding Meretek}" Margin="10,0,0,0" />
            <ComboBox SelectedValue="{Binding Columns, Mode=TwoWay}" ItemsSource="{Binding Meretek}" Margin="5,0,0,0" />
        </StackPanel>
        <StackPanel Margin="0,20,0,0"  DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Margin="5,0,0,0" Command="{Binding CreateNew}" Content="Üres matrix létrehozása" />
            <Button Margin="15,0,0,0" Command="{Binding Save}" Content="Állás mentése" />
        </StackPanel>
        <UniformGrid Name="CheckBoxGrid" Margin="20,20,20,20">
            
        </UniformGrid>
    </DockPanel>
</Window>
